import { createStore } from 'vuex'
import auth from "@/store/auth";
export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
    login({commit},{}){

    }
  },
  modules: {
    auth
  }
})
